# The application's dependencies. The 139 skills that scan, attack and govern
# this tree import none of them — they are standard library only, and CI proves
# it. These are here because the system under study is a real agentic
# application, and a real one speaks MCP and verifies a JWT.
fastapi>=0.115
uvicorn[standard]>=0.30
mcp>=1.2.0
PyJWT>=2.8
anthropic>=0.40          # only for the model loop; the planner runs without it
