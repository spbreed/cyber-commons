what the downstream service recorded:
   caller=svc-agent-7f3a1c  read    reports
   caller=svc-agent-7f3a1c  read    reports
   caller=svc-agent-7f3a1c  read    reports
   caller=svc-agent-7f3a1c  delete  prod.customers

incident: delete on prod.customers
which agent did it? candidates: ['deploy-agent', 'patch-agent', 'triage-agent']
distinguishable from the record? False

containment options:
   rotate svc-agent-7f3a1c -> stops the incident, and stops 3 agents including 2 innocent ones
   rotate only the culprit -> not available; there is no 'only'

No attacker forged anything. Impersonation is the resting state of a
system where identity was never per-workload.
