techniques, placed
   technique                     side   gate  blind to
   SAST / code analysis          pre    yes   whether the vulnerable path is reached with real traffic
   SCA and SBOM scanning         pre    yes   what actually loaded, versus what the manifest pinned
   IaC and policy-as-code        pre    yes   the identity and network the workload really got
   threat modelling              pre    -     entry points added by a config change, not a commit
   tool-schema / MCP review      both   yes   a description edited by the server after review
   DAST / attack simulation      post   -     the commit that introduced it
   egress enforcement            post   -     the config change that widened the allowlist
   runtime posture and drift     post   -     the plan the drift departed from
   detection and canaries        post   -     anything at all before it happens
   attestation                   both   -     nothing - it signs on the left and re-checks on the right

4 of 10 can block a merge. Everything else is
advisory - it produces a ticket, and a ticket is a request, not a control.

risks, by which side covers them
      injection in application code             covered
   -> vulnerable declared dependency            pre-only
   !! undeclared vendored binary                uncovered
   -> over-broad IAM role at runtime            post-only
   -> prompt injection through retrieved text   post-only
      tool schema edited after review           covered
   -> guardrail switched off after the demo     post-only
   -> secret committed to the repository        pre-only

pre-only  2   preventable, and invisible once it ships:
           nothing on the right would tell you it happened.
post-only 3   visible, and unpreventable: detection is the whole
           of the control, and the merge that caused it went through.
uncovered 1   nothing on either side. B2.7 is this row - an
           artefact with no manifest entry has no identifier, so the
           SCA pass on the left never had anything to look up.

All three of those render as one word on a tooling inventory. The middle
row is the one that gets signed off, because a control exists, it runs,
and it reports - and it reports afterwards.
