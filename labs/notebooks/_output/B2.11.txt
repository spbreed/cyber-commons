what this engagement was handed, and what each source establishes

  repository at a pinned commit    entry points, call graph, sinks
  dependency lock file             the versions really resolved
  IaC plan                         what is exposed, and to whom
  tool / MCP manifests             what each agent may call
  IAM policy + role bindings       the authority behind each caller
  API schema (OpenAPI)             the objects and verbs that exist
  prior findings                   what has already been refuted

paths from a real entry point to a sink

  AUTHZ OK  POST /bookings             handler -> svc.book -> db.bookings.write
            authn: session   authz: owner==caller
  AUTHN ONLY POST /refunds              handler -> svc.refund -> payments.refund
            authn: session   authz: NONE ON ANY HOP
  AUTHN ONLY GET  /bookings/{id}        handler -> svc.get -> db.bookings.read
            authn: session   authz: NONE ON ANY HOP
  AUTHZ OK  MCP  vendor.itinerary      tool_router -> svc.book -> db.bookings.write
            authn: none   authz: owner==caller
  AUTHN ONLY cron nightly_reconcile     job -> payments.refund
            authn: service_account   authz: NONE ON ANY HOP

sinks present in the tree      7
sinks reachable from an entry  5
  of those, authenticated only 3
sinks present but unreachable  2  (admin.reset_all, db.audit.purge)

the unreachable ones are REPORTED, not dropped:
  they are one route away from being findings, and the next feature
  that adds a route will not re-run this analysis on its own.

findings worth a tester's time — reachable, and nothing on the path
checks that this caller is entitled to this object:
  - payments.refund        from POST /refunds            (has session, which is not authorisation)
  - db.bookings.read       from GET  /bookings/{id}      (has session, which is not authorisation)
  - payments.refund        from cron nightly_reconcile   (has service_account, which is not authorisation)

that is the shape of every BOLA finding, and it is the shape of the
CyberTravels refund incident: the caller was authenticated throughout.
