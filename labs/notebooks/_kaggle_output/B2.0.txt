components wired: ['loop', 'tools', 'context', 'verifier', 'state', 'budget', 'orchestrator', 'telemetry']
agent claimed success : True
independently checked : True
steps                 : 3
   1. write_patch {'wrote': 'auth.py'}
   2. run_tests   {'passed': False, 'failing': ['test_login']}
   3. done        {'claim': 'fixed it'}

It reported success. The tests still fail. Nothing in that transcript
is a lie - the agent did write a patch, and then it said it was done.
claimed True  verified False
claimed True  verified True

Same model. Same loop. Same tools. The only difference between a harness
that reports the truth and one that reports its own optimism is one
component - and it is the cheapest one in the table.
ran 4 steps, then stopped: claimed=False

Without the ceiling this runs until something else stops it - a bill, a
rate limit, or an on-call engineer. The budget is what makes 'autonomous'
a bounded word.
property              value
identity              ci-sast-harness
tools                 ['done', 'run_tests', 'write_patch']
writes                ['write_patch']
credentials           ['repo:write']
runs_unattended       True
telemetry             True

tools that change state : ['write_patch']
unattended              : True
auditable               : True

Every question you would ask of an agent applies to the thing you just
built to review agents. A harness with repo:write running unattended is
a non-human identity, and it belongs in the inventory in A2 and E1.2.
