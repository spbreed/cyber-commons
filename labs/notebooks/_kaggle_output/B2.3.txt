token                     source        agent                 verdict
hf_liveTokenNotShown      10.2.0.11     ci-runner             normal use
hf_CANARY7Fq2mXvLpR8s     203.0.113.9   python-requests/2.31  CONFIRMED COMPROMISE
ghp_alsoLive              10.2.0.11     ci-runner             normal use
sk-CANARYd3Vn8yHc2Uae     203.0.113.9   python-requests/2.31  CONFIRMED COMPROMISE

canary hits: 2  false positives possible: 0
Not zero because the detector is good - zero because nothing legitimate
has any reason to touch these strings. That is the whole property, and
it is why a hit opens an incident rather than a ticket.
attribution carried on the hit: source 203.0.113.9, agent python-requests/2.31
   T-03  cheat attempt: flag readable in the scorer fixture  (scored 0.0)
   T-05  cheat attempt: answer key path guessable from the task id  (scored 0.0)

honeypot tasks: 2   cheat attempts: 2   rate: 100%

The rate is the product, not the individual catch. Read continuously it
is a leading indicator for the whole fleet; read after the run it is an
autopsy.
day   0   detection rate 100%   fires on contact
day   7   detection rate 100%   fires on contact
day  20   detection rate 100%   fires on contact
day  21   detection rate 0%   silent - and silence looks clean
day  40   detection rate 0%   silent - and silence looks clean
day  90   detection rate 0%   silent - and silence looks clean

with quarterly-or-faster rotation: ['100%', '100%', '100%', '100%']

An unrotated canary does not fail loudly. It reports a clean environment
that is merely well-mapped, which is a worse position than having no
canary at all, because you now believe something.
detector                  TP    FP  precision  threshold?
credential canary          2     0       1.00  none needed
entropy on artifacts      11    62       0.15  yes
honeypot task              6     0       1.00  none needed
write-volume anomaly      18   140       0.11  yes

detectors needing no threshold: ['credential canary', 'honeypot task']

Deception finds less. What it finds needs no triage, no tuning and no
argument - which is why it belongs beside the volume detectors rather
than instead of them.
