tool                  plane     scope     reversible
--------------------------------------------------------
search_findings       decision  self      True
read_source           decision  self      True
post_jira_comment     action    project   True
close_finding         action    project   True
open_pr               action    project   True
merge_pr              action    project   False
rotate_credential     action    org       False

decision plane (2): ['search_findings', 'read_source']
action plane   (5): ['post_jira_comment', 'close_finding', 'open_pr', 'merge_pr', 'rotate_credential']

The model can propose all seven. Only the five on the action plane
can change anything, and only if the control plane lets them through.
ungoverned manifest — blast radius: 55
   rotate_credential       40
   merge_pr                 6
   post_jira_comment        3
   close_finding            3
   open_pr                  3

The worst single call is rotate_credential: org-wide and irreversible.
An agent that can close a finding can also rotate the credential that
finding was about. Nobody decided that; it fell out of the tool list.
as deployed                    rung=L2.5  blast= 55  FINDINGS
                                ⚠ claims L2.5 (bounded) but reaches org-wide ungated: ['rotate_credential']
                                ⚠ irreversible and ungated (no after-the-fact review possible): ['merge_pr', 'rotate_credential']
gate the irreversible ones     rung=L2.5  blast=  9  CLEAN
gate every writer              rung=L2    blast=  0  CLEAN
someone edits the manifest on a Tuesday:
   added          ['rotate_credential']
   removed        []
   blast          9 → 49
   delta          40
   new_findings   ["claims L2.5 (bounded) but reaches org-wide ungated: ['rotate_credential']", "irreversible and ungated (no after-the-fact review possible): ['rotate_credential']"]

No pull request touched the agent's code. The blast radius doubled.
This diff, wired into CI, IS the living architecture review.
