standing grants — the quarterly access review:
   deploy-agent     ['artifact:read', 'deploy:prod']
   patch-agent      ['repo:read', 'repo:write']
   backup-runner    ['db:read', 's3:write']

The reviewer's only possible question: 'should this still exist?'
With no usage data attached, the honest answer is always 'probably'.

actual use in the last 90 days:
   deploy-agent     used   6×  → authority idle 99.99% of the time
   patch-agent      used 210×  → authority idle 99.51% of the time
   backup-runner    used  30×  → authority idle 99.93% of the time
at issue:
   deploy-agent   deploy:prod    ACTIVE   ttl=  0.6s used=0  reason='roll out fix for CVE-2026-1188'
   patch-agent    repo:write     ACTIVE   ttl= 60.0s used=0  reason='patch finding-4471'

after the deploy window closes:
   deploy-agent   deploy:prod    expired  ttl=  0.6s used=1  reason='roll out fix for CVE-2026-1188'
   patch-agent    repo:write     ACTIVE   ttl= 60.0s used=1  reason='patch finding-4471'
naive agent    : deploy-agent's deploy:prod grant expired after 0.3s ('rollout')
                 → on-call asks for a 24h TTL, and JIT is over.

resilient agent:
    step 0: ok
    step 1: ok
    step 2: grant expired → re-requesting
    step 2: ok
    step 3: ok
   final grant: deploy-agent   deploy:prod    expired  ttl=  0.3s used=2  reason='rollout (continuation, step 2)'

The audit trail now contains every continuation and its reason —
which is strictly more information than a standing grant ever produced.
