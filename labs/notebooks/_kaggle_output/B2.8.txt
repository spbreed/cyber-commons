round strategy   own judge  held-out truth 
----------------------------------------------
1     double     1.00       1.00           
2     double     1.00       1.00           
3     constant   1.00       0.00           
4     identity   1.00       0.00           
5     double     1.00       1.00           
6     constant   1.00       0.00           
round strategy   own judge  held-out truth verdict
--------------------------------------------------------------
1     constant   1.00       0.00           looks fine / actually BROKEN
2     constant   1.00       0.00           looks fine / actually BROKEN
3     identity   1.00       0.00           looks fine / actually BROKEN
4     double     1.00       1.00           looks fine / actually ok
✗ the scaffold reads its own logs, which contain held-out failures
    → it optimises against them without anyone deciding to

✗ held-out cases are added to the prompt as 'examples of hard tasks'
    → the most common leak, and it looks like good engineering

✗ the same corpus is used for both tuning and evaluation
    → the number becomes a training metric and rises forever

✗ a human adjusts the scaffold after reading held-out results
    → slower leak, same effect — the human is the gradient

as usually built   sound=False
      ⚠ scaffold can read held-out outcomes
      ⚠ held-out cases appear in the prompt
      ⚠ tuning and eval share a corpus
      ⚠ human closes the loop manually
after the fix      sound=True
REJECTED change: held-out 1.00 → 0.00
final strategy: double

The scaffold's own judge would have accepted the change. The held-out
oracle rejected it, which is the only reason the system still works.
