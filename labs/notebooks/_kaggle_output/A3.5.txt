M0 · no policy (framework default)
   read_file           allow
   search_code         allow
   write_file          allow
   open_pr             allow
   merge_pr            allow
   deploy_prod         allow
   rotate_credential   allow
   exec_python         allow

M1 · L2 — approve every writer
   read_file           allow
   search_code         allow
   write_file          approval
   open_pr             approval
   merge_pr            approval
   deploy_prod         approval
   rotate_credential   approval
   exec_python         approval

M2 · L2.5 — bounded set, some tools never
   read_file           allow
   search_code         allow
   write_file          allow
   open_pr             allow
   merge_pr            approval
   deploy_prod         deny
   rotate_credential   deny
   exec_python         deny

M3 · L1 — read only
   read_file           allow
   search_code         allow
   write_file          deny
   open_pr             deny
   merge_pr            deny
   deploy_prod         deny
   rotate_credential   deny
   exec_python         deny

a new tool appears and nobody updated the policy:
   M0 · no policy (framework default)     refused
   M1 · L2 — approve every writer         refused
   M2 · L2.5 — bounded set, some tools never refused
   M3 · L1 — read only                    refused

M0 permits it because its allowlist was 'everything known at the time'.
The other three refuse it, not because anyone anticipated run_terraform,
but because they refuse anything unlisted. That is the whole property.
PASS  M2 · L2.5                              blast=  6 budget= 12 (L2.5)
FAIL  M0 · no policy                         blast=108 budget= 12 (L2.5)
PASS  M3 · L1 read only                      blast=  0 budget=  0 (L1)
PASS  M2 loosened (merge_pr allowed)         blast= 12 budget= 12 (L2.5)
policies checked: 5 · tools: 9 · violations: 0
Invariant holds: deny is absolute, and nothing unlisted is ever permitted.
