   chain  : dana@corp -> orchestrator -> patch-agent
   scopes : ['reports:read']

priya holds db:admin. She asks the same low-trust agent to use it:
      (narrowed by patch-agent ceiling: dropped ['db:admin'])
   subset-of-presented alone would allow it : True
   scopes actually issued                   : none

Subset-only says yes - she really does hold db:admin. The ceiling rule
issues the intersection, which is empty, because patch-agent may never
hold it no matter who asks.
