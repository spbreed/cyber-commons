src/data
   units       ['load_report', 'store']
   entry pts   —
   talks to    —
   touches     ['database', 'filesystem']

src/util
   units       ['render']
   entry pts   —
   talks to    —
   touches     —

src/web
   units       ['get_report', 'upload_doc']
   entry pts   ['get_report', 'upload_doc']
   talks to    ['load_report', 'render', 'store']
   touches     —

ENTRY POINTS (untrusted input arrives here)
   get_report     src/web                HTTP request (untrusted)
   upload_doc     src/web                HTTP request (untrusted)

DATA FLOWS
   get_report → load_report
   get_report → render
   upload_doc → store

SINKS (state changes / external resources)
   load_report    execute    database
   store          open       filesystem
   store          write      filesystem
TRUST BOUNDARY CROSSINGS
   get_report → load_report    src/web    → src/data   (trust 0 → 2)
   get_report → render         src/web    → src/util   (trust 0 → 1)
   upload_doc → store          src/web    → src/data   (trust 0 → 2)

SINKS REACHABLE FROM AN ENTRY POINT
   get_report     → load_report    touches database
   upload_doc     → store          touches filesystem
   upload_doc     → store          touches filesystem
entry points before: ['get_report', 'upload_doc']
entry points after : ['admin_export', 'get_report', 'upload_doc']
NEW ENTRY POINT    : ['admin_export']
flows before 3 → after 5

One function added. A new untrusted entry point now reaches both the
database and the filesystem. That delta is what B1.3 threat-models.
loaded skill: appsec-repo-recon
  tools it may use: Read, Grep, Glob, Bash
  routing description: 53 words
  procedure: 88 lines
conformance check: 0 problem(s)

The map satisfies the contract, so Phase 2 can consume it without
negotiating a format.
hollow map, conformance problems: 0
real map,   conformance problems: 0

Both conform. One of them found nothing at all.

Conformance is a statement about the serialiser: it is close to free by
construction, and an empty result scores perfectly. Accuracy is the
expensive part and the contract cannot measure it. Any pipeline that
reports '100% schema-valid' as a quality metric is reporting this number.

what the contract can tell you : shape is right (0 problems)
what only the map can tell you : 3 reachable entry->sink pairs, 3 boundary crossings
map the attack surface of this repo before w   -> appsec-repo-recon  margin=3
what could go wrong with this architecture     -> appsec-threat-model  margin=3
is this alert a false positive                 -> AMBIGUOUS (tie at 2)  margin=0

The third routes with margin 0. 'false positive' appears in the audit
skill's description and 'alert' in the triage skill's, so both score the
same and the winner is whichever sorted first alphabetically - an
arbitrary answer wearing a confident face.

That is why route() returns the margin. A tie is a configuration bug in
the descriptions, and the fix is to make them disjoint, not to let the
sort decide which procedure runs.
