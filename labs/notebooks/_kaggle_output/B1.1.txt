file                      commits  sec fixes    risk
----------------------------------------------------
src/api/bookings.py             2          2   0.532
src/data/docs.py                2          1   0.311
src/data/reports.py             3          1   0.189
requirements.txt                1          0     0.0
src/util/render.py              3          0     0.0

src/api/bookings.py and src/data/docs.py are the repeat zones, and the
ordering comes entirely from history - no rule has run.
Note what stage 1 missed: 'harden the voucher path join' is the most
recent security commit in the list and matches no marker, so it scores
nothing. Marker quality IS the accuracy of this stage.
unit            file                     line  params -> calls
------------------------------------------------------------------------------
get_booking     src/api/bookings.py         2  ['request'] -> ['load_booking', 'render']
upload_voucher  src/api/bookings.py         6  ['request'] -> ['store']
store           src/data/docs.py            2  ['blob', 'name'] -> ['open', 'write']
load_booking    src/data/reports.py         2  ['ref', 'owner'] -> ['execute']
render          src/util/render.py          2  ['rows'] -> ['join', 'str']

"who reaches store(), and with what?"
   line-based index : cannot answer - the token appears in two places
   file-based index : "it is in src/data/docs.py"
   semantic index   : callers = ['upload_voucher'], and its input is a traveller-supplied filename

entry points (nothing in the repository calls them): ['get_booking', 'upload_voucher']
model backend : replay
model         : deterministic stand-in (no backend configured)

This lesson runs offline against a deterministic replay, which is why
it works on a Kaggle kernel with the internet switched off. To run the
identical code against a real model, set one of:

   frontier     export ANTHROPIC_API_KEY=...   # cheapest: claude-haiku-4-5-20251001
                (an identity-linked key also needs
                 ANTHROPIC_WORKSPACE_ID=...)
   open weight  export OPENAI_BASE_URL=http://localhost:11434/v1 \
                       OPENAI_API_KEY=ollama MODEL=glm-4.6

   On Kaggle: Add-ons -> Secrets, add ANTHROPIC_API_KEY, and switch
   Internet on in the notebook settings. Internet requires a
   phone-verified Kaggle account; without it DNS fails in the kernel
   and this lesson correctly stays on the replay.
backend used : replay
model        : deterministic stand-in (no backend configured)
prompt       : Summarise what this component does in one sentence, then name its ...

answer:
   Accepts traveller HTTP requests for bookings and voucher uploads, passing both straight into the data layer.
   Trust boundary: every parameter here arrives from the public internet, so the edge between src/api and src/data is where untrusted input crosses into a component that reaches the database and the filesystem.

property checked : names a trust boundary
held on replay       : True

Same code, same assertions, three possible backends. Offline the answer
is the replay and is labelled as one; with a key it is the model's.
src/api
   units       ['get_booking', 'upload_voucher']
   entry pts   ['get_booking', 'upload_voucher']
   talks to    ['load_booking', 'render', 'store']
   touches     -

src/data
   units       ['store', 'load_booking']
   entry pts   -
   talks to    -
   touches     ['database', 'filesystem']

src/util
   units       ['render']
   entry pts   -
   talks to    -
   touches     -

ENTRY POINTS    ['get_booking', 'upload_voucher']
DATA FLOWS
   get_booking -> load_booking
   get_booking -> render
   upload_voucher -> store
SINKS
   load_booking    touches the database
   store           touches the filesystem
TRUST BOUNDARY CROSSINGS
   get_booking -> load_booking       src/api (trust 0) -> src/data (trust 2)
   get_booking -> render             src/api (trust 0) -> src/util (trust 1)
   upload_voucher -> store           src/api (trust 0) -> src/data (trust 2)

SINKS REACHABLE FROM AN ENTRY POINT
   get_booking      -> load_booking     touches the database
   upload_voucher   -> store            touches the filesystem
entry points before : ['get_booking', 'upload_voucher']
entry points after  : ['admin_export', 'get_booking', 'upload_voucher']
NEW                 : ['admin_export']
flows 3 -> 5, boundary crossings 3 -> 5

One function. A new untrusted entry point now reaches both the database
and the filesystem, and two new edges cross a trust boundary. A threat
model drawn in a workshop last quarter says nothing about any of it.
loaded skill: appsec-repo-recon
  tools it may use: Read, Grep, Glob, Bash
  routing description: 53 words
  procedure: 88 lines
