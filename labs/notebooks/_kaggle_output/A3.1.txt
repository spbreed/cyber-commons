allow-by-default:
   run_query  table:reports         SELECT ALLOW rule reports-agent/run_query/table:reports
   run_query  table:secrets         SELECT ALLOW allowed by default
   run_query  table:reports         DELETE deny  verb DELETE not permitted on table:reports
   send_email domain:evil.example   *      ALLOW allowed by default
   drop_table table:reports         *      ALLOW allowed by default
   -> 4/5 permitted

DEFAULT-DENY:
   run_query  table:reports         SELECT ALLOW rule reports-agent/run_query/table:reports
   run_query  table:secrets         SELECT deny  no rule matches - default deny
   run_query  table:reports         DELETE deny  verb DELETE not permitted on table:reports
   send_email domain:evil.example   *      deny  no rule matches - default deny
   drop_table table:reports         *      deny  no rule matches - default deny
   -> 1/5 permitted

The only call that should succeed is the first. Under allow-by-default
four do, and each one is a real risk from Chapter 1 walking through.

Note the third row: same identity, same tool, same resource, refused on
the verb. Authorization attached to the tool instead of the call cannot
express that distinction at all.
