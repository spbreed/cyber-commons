internal-api    accepts obo       — modern, RFC 8693 aware
github          accepts bearer    — PAT or app token, one identity
mainframe-fin   accepts svc-acct  — fixed service account, 1998

agent presents: dana@corp → orchestrator → patch-agent  scopes=['ledger:post', 'repo:write']
internal-api    actor=patch-agent                  principal=dana@corp  chain=['dana@corp', 'orchestrator', 'patch-agent']
github          actor=ghs_gateway_scoped_to_repo   principal=unknown    chain=LOST
mainframe-fin   actor=SVCACCT-GW-01                principal=unknown    chain=LOST

Two of three downstreams cannot record who caused the action.
Worse: to them, every agent looks like the same service account —
so a per-agent policy CANNOT be enforced there. It has to happen earlier.
permitted actor              ALLOW github          ghs_gateway_scoped_to_repo_write
wrong actor for this route   DENY  mainframe-fin   actor patch-agent not permitted on this route (allowed: ['finance-agent'])
correct actor                DENY  mainframe-fin   delegation depth 3 > 2
chain too deep               DENY  github          delegation depth 5 > 3
gateway audit log — the only place the full story exists:
   ALLOW github          dana@corp → orchestrator → patch-agent
   DENY  mainframe-fin   dana@corp → orchestrator → patch-agent
   DENY  mainframe-fin   dana@corp → orchestrator → finance-agent
   DENY  github          dana@corp → orchestrator → sub-2 → sub-3 → patch-agent

1/4 calls allowed; every one carries its chain,
including the two whose downstream saw only a service account.
