.venv/
# cloned vulnerable repos live under ground-truth/ but are not committed;
# the folder's README is tracked
ground-truth/*/
logs/
data/vulnbench.db
__pycache__/
*.pyc

# transient Google Mantis clone (schema is vendored at bench/mantis_schema.json)
_mantis/
# NOTE: the blind corpora (work_mantis/blind*, copies of public SecLLMHolmes
# code) and the answer keys (work_mantis/.labels*.json) ARE committed as
# reproducibility evidence — deliberately not ignored.
